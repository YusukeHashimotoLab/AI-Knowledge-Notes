# Kept only as a compatibility shim for older docs/scripts that installed the
# pipeline dependencies from tools/. There is exactly one dependency list in this
# repository: wp/requirements.txt. Edit that file, not this one.
#
#   python3 -m pip install -r requirements.txt                      # from wp/
#   python3 -m pip install -r tools/requirements-markdown-pipeline.txt  # equivalent
-r ../requirements.txt
